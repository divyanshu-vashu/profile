# DevEnv Automator

DevEnv Automator helps developers onboard in under 60 seconds. Using a single configuration file, it provisions isolated Docker containers pre-configured with editor plugins, compilers, and debuggers.

## Features
- **Config-driven**: Simple `.yaml` declarations.
- **Docker-backed**: Uniform runtime execution environment.
- **Go binary**: Extremely fast, dependency-free compilation.
